@model IEnumerable<CASMUL.Models.Cables.ListCableViewModel>



<div class="row">

    <div class="col-md-12 ui-sortable">
        <!-- begin panel -->
        <div class="panel panel-inverse">
            <div class="panel-heading">
                <div class="panel-heading-btn">
                    <a href="@Url.Action("Create","Entrega")" class="btn btn-xs btn-primary" data-original-title="Crear Usuario" title="Crear Usuario"><i class="fa fa-plus"></i> Nuevo</a>
                </div>
                <h4 class="panel-title">Cables</h4>
            </div>
            <div class="panel-body">
                <table class="table" data-class="dataTable">
                    <thead>
                        <tr>
                            <th>
                                @Html.DisplayNameFor(model => model.Acres)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.Hectarias)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.Descripcion)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.IdGrupo)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.Finca)
                            </th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(model => item.Acres)
                                </td>
                                <td>
                                    @Html.DisplayFor(model => item.Hectarias)
                                </td>
                                <th>
                                    @Html.DisplayFor(model => item.Descripcion)
                                </th>
                                <td>
                                    @Html.DisplayFor(model => item.Grupo)
                                </td>
                                <td>
                                    @Html.DisplayFor(model => item.Finca)
                                </td>
                                <td>
                                    @Html.ActionLink("Edit", "Edit", new { id = item.IdCable }) |
                                    @if (Convert.ToBoolean(item.Activo))
                                    {
                                        @Html.ActionLink("Desactivar", "Delete", new { id = item.IdCable })
                                    }
                                    else
                                    {
                                        @Html.ActionLink("Activar", "Delete", new { id = item.IdCable })
                                    }
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>

            </div>

        </div>
    </div>
</div>

@section Scripts {
    <script>
        $('table').dataTable();
    </script>
}